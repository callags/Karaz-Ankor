<ul id="menu">
	<li><%= render 'homepage/banner' %></li>
	<li><a href="/empire#lost-hold">Back to the Dwarven Empire</a></li>
</ul>:

<div class="title_page">
	<h1>Karak Ungor</h1>
</div>

<%= image_tag("lost-dwarfhold-banner.png", :class => "leftsidepicture-with-background") %> <%= image_tag("ungor.jpg", :class => "rightsidepicture", style:"height:250px") %>

<% @articles.each do |article| %>
	<% if article.title == "ungor pt.1" %>
		<p>
			<h2><%= article.text %></h2>
		</p>
	<% end %>
<% end %>
<br>
<% @articles.each do |article| %>
	<% if article.title == "ungor pt.2" %>
		<p>
			<h2><%= article.text %></h2>
		</p>
	<% end %>
<% end %>
<br>
<% @articles.each do |article| %>
	<% if article.title == "ungor pt.3" %>
		<p>
			<h2><%= article.text %></h2>
		</p>
	<% end %>
<% end %>
<br><br><br>

<%= render 'homepage/banner' %>