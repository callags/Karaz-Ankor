<%= render :template => "q1/index" %>

<% if @segment_sub_1a == 1 %>
	<center>
		<h2>
			<div id="fade">
				<% if not ( @url.include?('1a1') or @url.include?('1a2')) %>
					<%= "1. Use your gun." %>
					<br><br>
					<%= "2. Use your throwing axes." %>
					<br><br>
					<%= form_for("pathfinder/a/q1/#{@user_input}/q1s1", method: :post) do %>
						<%= label_tag(:section_sub1_input, "Please choose a path:") %>
						<%= text_field_tag :section_sub1_input, @response_sub1, maxlength:1 %>
						<%= submit_tag "Submit", :id => "submit", 'data-disable-with' => "Please wait.." %>
					<% end %>
					<br>
				<% end %>
			</div>
		</h2>
	</center>
<% end %>